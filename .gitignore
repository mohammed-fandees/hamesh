# Dependencies
node_modules/
.pnpm-store/

# Build output (WXT)
.output/
.wxt/
web-ext-artifacts/
*.zip

# Test / coverage artifacts
coverage/
test-results/
playwright-report/
playwright/.cache/
.e2e-user-data*/
.shots/
.playwright-shots/

# Environment & secrets — never commit
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / IDE
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local scratch / debug files
*.local
hm-*.mjs

# Generated recording artifacts (large binaries — never commit video output)
artifacts/

# Scratch notes kept at the repo root while drafting store copy. The real,
# reviewed permission justifications live in
# docs/chrome-web-store/PERMISSION_JUSTIFICATIONS.md — a working draft
# beside them should never be committed by a stray `git add -A`.
/hamesh-*-permission-justification.md
